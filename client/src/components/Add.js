import React from 'react';
import ReactDOM from 'react-dom';

import HaikuForm from '../containers/HaikuForm';

const Add = () => {
  return(
    <section>
      <HaikuForm />
    </section>
  );
}

export default Add;
